import { describe, it, expect } from "bun:test";

describe("Sign in", () => {
	it.todo("should set default email input value if email is present on search params", () => {

    expect("ok").toBe("ok")
  });
});
